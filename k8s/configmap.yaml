apiVersion: v1
kind: ConfigMap
metadata:
  name: ecommerce-config
data:
  DB_HOST: "db"
  DB_PORT: "5432"
  DB_NAME: "${DB_NAME}"
  DB_USER: "${DB_USER}"
  APP_URL: "${APP_URL}"
  KEY_SERVICE_URL: "${KEY_SERVICE_URL}"
  G_URL: "${G_URL}"
  G_API_URL: "${G_API_URL}"